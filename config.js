/* ============================================================
   ONE V ONE — API configuration
   Change this to your deployed backend URL before going live,
   e.g. "https://one-v-one-api.onrender.com/api"
   ============================================================ */
window.OVO_API_BASE = window.OVO_API_BASE || "http://localhost:4000/api";
